{
  "name": "hardhat-project",
  "devDependencies": {
    "@chainlink/contracts": "^1.2.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@pythnetwork/pyth-sdk-solidity": "^4.0.0",
    "hardhat": "^2.22.10"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run ignition/modules/Lock.ts",
    "clean": "hardhat clean",
    "node": "hardhat node"
  }
}
